# dap-dbt-transformations-spec-compliant

Reference implementation of a dbt project with full compliance to the
[dap-dwh-spec](https://github.com/vicwik-gyg/dap-dwh-spec) standards.

## What this is

A shadow repo containing ~32 dbt models covering all 11 entity archetypes from
the spec. Every model is checked by `shadow-spec-check` on every PR. The data is
seeded from CSVs — no production data access required.

**This repo targets the `testing` catalog only.** There is no path to production.

## Why it exists

The real `dap-dbt-transformations` repo has 1,228 models. Applying the spec to
all of them at once is impractical. This shadow repo proves the spec is
implementable and serves as a template for migrating the real repo in phases.

## Quick start

```bash
# Prerequisites: Python >= 3.12, uv, Databricks CLI with OAuth configured
uv sync
make all    # deps + seed + run + test
```

See [HOW-TO-RUN.md](HOW-TO-RUN.md) for detailed setup and troubleshooting.

## Key docs

| Doc | Purpose |
|-----|---------|
| [docs/HOW-TO-AUDIT.md](docs/HOW-TO-AUDIT.md) | How to check whether a model is spec-compliant |
| [docs/HOW-TO-MIGRATE.md](docs/HOW-TO-MIGRATE.md) | How to migrate a source model into this repo |
| [HOW-TO-RUN.md](HOW-TO-RUN.md) | Environment setup, build commands, troubleshooting |
| [docs/SPEC-CHECK.md](docs/SPEC-CHECK.md) | CLI reference and how to add new rules |
| [docs/COMPLIANCE-AUTO.md](docs/COMPLIANCE-AUTO.md) | Auto-generated compliance matrix (regenerated by CI) |

## External references

- [dap-dwh-spec](https://github.com/vicwik-gyg/dap-dwh-spec) — the standard itself
- [MIGRATION-PLAYBOOK](https://github.com/vicwik-gyg/dap-dwh-spec/blob/main/04-target-state/MIGRATION-PLAYBOOK.md) — conventions reference for what a migrated model looks like

## Safety

- **Testing catalog only** — `dbt_project.yml` is configured for testing schemas
- **Schema prefix** — all schemas start with `spec_compliant_` to avoid collisions
- **No production path** — if you see `production` in any config, stop and open a P0 task
- **Seed-based** — all source data comes from CSV seeds, no external dependencies
